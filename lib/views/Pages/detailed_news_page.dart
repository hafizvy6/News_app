import 'package:flutter/material.dart';

class DetailedNews extends StatelessWidget {
  const DetailedNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Detailed News Page'));
  }
}
