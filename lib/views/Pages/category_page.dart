import 'package:flutter/material.dart';

class CatergoryPage extends StatelessWidget {
  const CatergoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Category Page'));
  }
}
