import 'package:flutter/material.dart';

class RecomendationTopic extends StatelessWidget {
  const RecomendationTopic({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
